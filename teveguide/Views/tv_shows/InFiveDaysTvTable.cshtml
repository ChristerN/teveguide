@model IEnumerable<teveguide.Models.db.tv_shows>
@{
    ViewBag.Title = "På den här kanalen om fem dagar";
}
<h2>@ViewBag.Title</h2>

<h3>@DateTime.Today.AddDays(5)</h3>

<table class="table">

    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Starttime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Category)
        </th>
    </tr>
    @foreach (var item in Model)
    {
        if (item.Starttime > DateTime.Today.AddDays(5) && item.Endtime < DateTime.Today.AddDays(6))
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Title)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Starttime)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Category)
                </td>
                <td>
                    @Html.ActionLink("Mer info", "Details", new { id = item.Id })
                </td>
            </tr>
        }
    }

</table>

<p>@Html.ActionLink("På den här kanalen om sex dagar", "InSixDaysTvTable")</p>

<p>@Html.ActionLink("På den här kanalen om fyra dagar", "InFourDaysTvTable")</p>

<p>@Html.ActionLink("Till startsidan", "Index")</p>
